import { BibleReferenceDatasource } from './bible-reference-datasource';

describe('BibleReferenceDatasource', () => {
  it('should create an instance', () => {
    expect(new BibleReferenceDatasource()).toBeTruthy();
  });
});
